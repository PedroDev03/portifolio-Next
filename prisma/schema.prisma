datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

// Seus modelos continuam abaixo...
model Funcionario {
  id     Int    @id @default(autoincrement())
  nome   String
  setor  String
  funcao String
}